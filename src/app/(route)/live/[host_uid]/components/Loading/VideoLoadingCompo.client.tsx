const VideoLoadingCompo = () => {    
    return (
        <div style={{maxHeight:"calc(-226px + 100vh)"}} className="w-full aspect-[100/55] bg-black"/>
    );
};

export default VideoLoadingCompo;
    